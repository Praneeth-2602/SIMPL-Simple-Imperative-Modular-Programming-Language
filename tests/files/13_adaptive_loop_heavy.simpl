let i be 5
let j be 3
let acc be 0
let outer be 0

while i > 0 do
    set outer to outer + i
    while j > 0 do
        set acc to acc + i + j
        set j to j - 1
    end
    set i to i - 1
    set j to 3
end

print acc
print outer
